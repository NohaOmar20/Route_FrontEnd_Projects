The main task point is applying CRUD by a Add contact Form
In the Buttom and the form, I used the Modal from Bootstrap 
In the button it will have the attribute data-bs-toggle=Modal
and in the div class=modal too And both having the same id 

Important note in update function 
index !== currentIndex:  the duplicate check now uses contacts.find(function(c, index) { ... }). 
This ensures that if the user updates a contact's name but keeps their phone number the same, 
it won't flag themselves as a duplicate.